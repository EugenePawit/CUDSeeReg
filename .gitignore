# Dependencies
node_modules/

# Build output
dist/

# TypeScript incremental build cache (must not be committed — causes
# nondeterministic "passes locally, fails in CI" builds)
*.tsbuildinfo

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
bun-debug.log*

# Environment
.env
.env.local
.env.*.local

# Project-specific
CLAUDE.md

# Local tooling (machine-specific paths)
.claude/

# Local docker overrides (e.g. publishing the API port for testing)
docker-compose.override.yml
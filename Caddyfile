# Serve over HTTP (Cloudflare Tunnel handles HTTPS)
# Cloudflare Tunnel forwards traffic to this port

:8080 {
    root * /usr/share/nginx/html
    encode gzip
    try_files {path} /index.html
    header {
        X-Frame-Options "SAMEORIGIN"
        X-Content-Type-Options "nosniff"
    }
}